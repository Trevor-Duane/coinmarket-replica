/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images:{
        domains: [
            'www.crypto-nation.io',
        ],
    },
};

module.exports = nextConfig
